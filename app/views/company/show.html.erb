<p>
  <strong>Name:</strong>
  <%= @company.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @company.description %>
</p>

<%if can? :update, @company%>
  <%= link_to 'Edit', edit_company_path(@company), class: "btn btn-secondary" %> |
<%end%>
<%= link_to 'Back', company_index_path, class: "btn btn-secondary" %>